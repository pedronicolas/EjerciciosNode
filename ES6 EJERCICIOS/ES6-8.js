let username = process.argv[2];
let comment = process.argv[3];
let final = '';
function html(argment) {                                                                                                                                                      // don't forget to return the escaped string!                                                                                                                       
    for (let i = 0; i < argment.length; i++) {
    let variable = argment[i];    
        switch (variable) {
            case "'":
                
                break;
        
            default:
                break;
        }
        
    }
}